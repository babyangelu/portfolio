spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/portfolio?allowPublicKeyRetrieval=true&useSSL=false
    username: portfolio
    password: portfolio
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
